name: Release
on:
  push:
    tags:
      - "v*"
jobs:
  release:
    uses: eeekcct/goreleaser-rust-workflow/.github/workflows/release.yml@v0.1.0
    permissions:
      contents: write
      id-token: write
      actions: read
      attestations: write
